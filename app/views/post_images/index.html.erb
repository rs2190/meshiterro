<!--app/views/post_imagesフォルダのindex.html.erbファイルで、部分テンプレートを呼び出せるように設定します。 以下のように記述します。-->

<%= render 'list', post_images: @post_images %>

<!--この一行について解説すると下のようになります。-->

<!-- %= render [部分テンプレートファイルの指定], [ローカル変数]:[渡す値] %>-->

<!--テンプレートファイルを呼び出す記述-->

 <!--部分テンプレートファイルを呼び出すには、renderを使う-->
 <!--'list'で部分テンプレートのパスを記述する-->
 <!--その際、アンダースコアは省略する-->
 <!-- また、post_images/list.html.erbが本来の呼び出し方だが、拡張子は省略でき、同じフォルダ内から呼び出す場合はフォルダ名も省略できる-->
 <!-- ローカル変数（'post_images'）に渡すインスタンス変数（@post_images）を指定する-->